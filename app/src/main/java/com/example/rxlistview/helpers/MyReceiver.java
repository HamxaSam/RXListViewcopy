package com.example.rxlistview.helpers;import android.app.NotificationManager;import android.content.BroadcastReceiver;import android.content.Context;import android.content.Intent;import androidx.core.app.NotificationCompat;import androidx.work.OneTimeWorkRequest;import androidx.work.WorkManager;import com.example.rxlistview.R;public class MyReceiver extends BroadcastReceiver {    @Override    public void onReceive(Context context, Intent intent) {//        OneTimeWorkRequest oneTimeWorkRequest = new OneTimeWorkRequest.Builder(WorkingPeriods.class).build();//        WorkManager.getInstance().enqueue( oneTimeWorkRequest);        showNotification(context,"Inducesmile", "Hello world, work completed on time by broadcast reciever");    }    private void showNotification(Context context, String title, String task) {        NotificationManager notificationManager = (NotificationManager)context. getSystemService( Context.NOTIFICATION_SERVICE);        NotificationCompat.Builder notification = new NotificationCompat.Builder(context, "inducesmile")                .setContentTitle(title)                .setContentText(task)                .setSmallIcon( R.mipmap.ic_launcher);        notificationManager.notify(1, notification.build());    }}